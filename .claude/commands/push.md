# Push to Remote

Push current branch to remote repository.

## Instructions

1. First run `git status` to check current state
2. If there are uncommitted changes, warn the user
3. Push to remote with `git push`
4. If upstream is not set, use `git push -u origin <current-branch>`
5. Report the result to the user
